@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Xác minh mã khôi phục";
}

<h1 style="text-align: center;">@ViewData["Title"]</h1>
<div class="ui centered grid">
    <div class="column" style="max-width: 450px;">
        <div class="ui segment">
            <form method="post" class="ui large form">
                <div class="field">
                    <a asp-page="./Login"><i class="icon arrow left"></i>Quay lại</a>
                </div>
                <div class="field">
                    <p>
                        Bạn đã yêu cầu đăng nhập bằng mã khôi phục. Thông tin đăng nhập này sẽ không được ghi nhớ
                        cho đến khi bạn cung cấp mã ứng dụng xác thực khi đăng nhập hoặc tắt 2FA và đăng nhập lại.
                    </p>
                </div>
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                <div class="field">
                    <label asp-for="Input.RecoveryCode"></label>
                    <input asp-for="Input.RecoveryCode" autocomplete="off" placeholder="Mã khôi phục" />
                    <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
                </div>
                <button type="submit" class="fluid ui large primary button">Xác minh</button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}